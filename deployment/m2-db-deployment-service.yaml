apiVersion: apps/v1
kind: PersistentVolume
metadata:
  name: m2-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/m2-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: m2-db-pvc
  labels:
    app: mysql
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: m2-db-deployment
  labels:
    app: m2-db-deployment
    tier: database
spec:
  replicas: 1
  selector:
    matchLabels:
      app: m2-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: m2-db-deployment
        tier: database
    spec:
      containers:
        - name: m2-db
          image: mysql:8.0
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-credentials
                  key: DB_PASSWORD_M2
            - name: MYSQL_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: mysql-config-map
                  key: DB_DBNAME_M2
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: mysql-credentials
                  key: DB_USER_M2
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-credentials
                  key: DB_PASSWORD_M2
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: m2-db-data
            - mountPath: /docker-entrypoint-initdb.d/init.sql
              name: m2-db-sql
              subPath: m2-db.sql
      volumes:
        - name: m2-db-data
          persistentVolumeClaim:
            claimName: m2-db-pvc
        - name: m2-db-sql
          configMap:
            name: m2-db-sql-config
---
apiVersion: v1
kind: Service
metadata:
  name: m2-db-service
  labels:
    app: m2-db-service
    tier: database
spec:
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  selector:
    app: m2-db-deployment
    tier: database
  type: ClusterIP
